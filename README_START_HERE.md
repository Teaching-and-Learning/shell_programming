# Start Here

Open the Replit **Shell** and try these commands first:

```bash
pwd
ls
ls -lF
cd unix_basics
pwd
ls -lF
cat documents/notes.txt
grep -F UNIX data/search.txt
cd ..
```

Then open `unix_basics/README_UNIX_BASICS.md` and complete the tasks.
